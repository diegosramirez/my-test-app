import { Routes } from '@angular/router';
import { RelativeTimeDemoComponent } from './relative-time-demo.component';

export const routes: Routes = [
  { path: '', component: RelativeTimeDemoComponent },
  { path: 'demo', component: RelativeTimeDemoComponent }
];
