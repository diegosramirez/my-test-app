import { Routes } from '@angular/router';
import { TextboxPageComponent } from './pages/textbox-page/textbox-page';

export const routes: Routes = [
  { path: 'textbox', component: TextboxPageComponent },
];
